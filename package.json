{
    "private": "true",
    "scripts": {
        "postinstall": "dotnet tool restore",
        "start": "dotnet run -c Release --project ../Fable/src/Fable.Cli -- watch src/App -s -o dist -e .jsx --run vite"
    },
    "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    },
    "devDependencies": {
        "vite": "^3.1.0",
        "@vitejs/plugin-react": "^2.1.0"
    }
}
